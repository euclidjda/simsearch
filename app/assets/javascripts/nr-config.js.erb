/*
 * Configuration constants that vary by environment.
 */

var NR = NR || {};
NR.config = {};


NR.config.SERVER_URL = "/";
// NR.config.FB_APP_ID // not available at asset precompile time.

NAU.log("Configuration complete")

