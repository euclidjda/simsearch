
<header class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">

      <!--
      <%= link_to (image_tag "", :id => 'bannerLogo', :alt => 'Logo'), root_path, id: "logo" %>
      -->
      <nav>
        <ul class="nav pull-right">
          <li class="banner-link"><%= link_to "Overview",     root_path %></li>
          <li class="banner-link"><%= link_to "Contact",  "mailto:" %></li>
          <% if current_user %>
            <li class="dropdown banner-link">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#">
              Hello,<b><%= current_user_display_name %></b><br>
              Your account<b class="caret"></b></a>
              <ul class="dropdown-menu">
                <li><a href="#">Profile</a></li>
                <li class="divider"></li>
                <li><%= link_to "Sign Out", '#', :id => 'banner-logout-btn' %></li>
              </ul>
            </li>  
          <% else %>
            <li class="banner-link"><%= link_to "Sign in", '#', :id => 'banner-login-btn' %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>

<% if current_user %>
  <div class="tabbable top_level_navigation_bar">
    <ul class="nav nav-tabs">
      <li class="<%= 'active' if current_page?(home_path) %> span3">
          <%= link_to "Overview", root_path %>
      </li>
    </ul>
  </div>
<% end %>

<!--
-->